services:
  nb:
    build: .
    image: nb-cli
    ports:
      - "6789:6789"
    volumes:
      - ./notes:/notes
    environment:
      - NB_DIR=/notes
      - HOME=/root
    stdin_open: true
    tty: true
    working_dir: /notes
